services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: golftracking-app
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/app/data
    volumes:
      - ${GOLF_DATA_PATH:-./data}:/app/data
    restart: unless-stopped
